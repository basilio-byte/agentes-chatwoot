# O entrypoint roda em Alpine. Se o Git converter para CRLF no Windows, o
# shebang quebra com "no such file or directory" dentro do container.
*.sh text eol=lf
Dockerfile text eol=lf
docker-entrypoint.sh text eol=lf
